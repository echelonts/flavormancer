# frontend/

React — the workbench UI. A chemist enters a molecule (name or SMILES) and sees
the full flavor read: taste meters, aroma descriptors, behavior, and safety flags,
each labeled with its confidence tier.

Built against the API's fixed JSON contract.
